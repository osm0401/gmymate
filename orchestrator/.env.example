# orchestrator/.env — API 키가 필요 없다. 세 역할 모두 CLI 계정 인증을 쓴다.
#   지휘 codex (ChatGPT 구독) / 코딩 claude / 리뷰 gemini (구글 계정)

BASE_BRANCH=main

# 선택 — 기본값이 있어 비워둬도 동작한다
# CODEX_MODEL=gpt-5.6-sol   # codex CLI 자체 기본값(주기적으로 바뀜)이 이 ChatGPT 계정
                            # 등급에서 못 쓰는 모델일 수 있다("... requires a newer
                            # version of Codex" / "not supported ... ChatGPT account").
                            # CLI를 최신으로 올려도 계정 제약이면 안 풀리니, 비워두다가
                            # 지휘 단계가 이런 에러로 죽으면 실제로 되는 모델명을 채워라.
# GEMINI_REVIEW_MODEL=gemini-2.5-pro
# GEMINI_AGY_MODEL=gemini-2.5-flash-lite
# REPO_DIR=..
# CLAUDE_PERMISSION_MODE=acceptEdits
# MAX_ROUNDS=20            # 0 = 무제한 (계획서 원안). 기본 20은 폭주 방지용 상한
